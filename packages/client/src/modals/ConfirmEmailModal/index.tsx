import React from 'react'

import { Flexbox } from '@stage-ui/core'

type ConfirmPhoneModalProps = {
  onClose: () => void
}

export const ConfirmEmailModal = ({ onClose }: ConfirmPhoneModalProps) => (
  <Flexbox>confirm email</Flexbox>
)
